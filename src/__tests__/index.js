import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Table from '../';

describe('Table', () => {
  it('test typeof Table', () => {
    expect(typeof Table).toEqual('function');
  });
});
